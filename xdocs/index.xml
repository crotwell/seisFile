<document>
  <properties>
    <title>SeisFile</title>
  </properties>
  <body>
    <section name="SeisFile">
      <p>
       This is a library of utilities for reading various seismic file types.
       Current support includes SAC, mseed and PSN.
         </p>
      <p>The jar file can be downloaded <a href="/software/mavenRepository/seisFile/jars">here.</a></p>
    </section>
    <section name="SAC">
       <p>SAC files can be read with code similar to the following.</p>
       
<source>
SacTimeSeries sac = new SacTimeSeries();
sac.read(filename);
</source>
     </section>
     <section name="mseed">
     <p>Miniseed support is only for straight miniseed, ie only binary "data records" and no ascii "control records". A mseed file can be read like this.</p>
<source>
DataInputStream dis = new DataInputStream(new BufferedInputStream(new FileInputStream(filename)));
MiniSeedRead mseed = new MiniSeedRead(dis);
try {
    while (true) {
        SeedRecord sr = mseed.getNextRecord();
    }
} catch (EOFException e) {
}
</source>
     </section>
  </body>
</document>
