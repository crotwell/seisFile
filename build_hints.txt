

./gradlew clean iD check assemble

# if client code has changed, may need to rerun man templates for picocli
#
./gradlew genManTemplate
# and check git diff on src/doc/man-templates

# check for dep updates
./gradlew dependencyUpdates

# wrapper
./gradlew wrapper --gradle-version 8.14.3 --distribution-type bin

# jreleaser
#   just to mavenCentral:
./gradlew jreleaserDeploy
#
# then log onto central.sonatype.com, may have to click the "Publish" button and refresh page till published.
# Jreleaser will keep trying Attempt X of 101 till published.
#
#   or the full thing, but maybe check github token?
./gradlew jreleaserFullRelease

# create tag
git tag -a -m "v2.3.3" v2.3.3

# Release
move the curr_release branch, github pages builds from this branch

# Github
create new release from tag, add release notes, append the zip from build/distributions

# homebrew
# Update formula with new url and sha256, can copy sha256 from github release

# Add build/distribution zip to github release before publish so
# zenodo will pick it up.

# Zenodo
# not-auto from github release, create new release and include the zip
